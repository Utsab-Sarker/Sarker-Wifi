
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - Sarker WiFi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-warning text-dark">
                        <h4 class="mb-0">Password Recovery</h4>
                    </div>
                    <div class="card-body">
                        <% if (error) { %>
                            <div class="alert alert-danger"><%= error %></div>
                        <% } %>

                        <% if (success) { %>
                            <div class="alert alert-success">
                                <h6>Account Information Found:</h6>
                                <p><strong>Name:</strong> <%= userData.name %></p>
                                <p><strong>Mobile:</strong> <%= userData.mobile %></p>
                                <p><strong>Password:</strong> Contact admin for password reset</p>
                                <div class="mt-3">
                                    <a href="/login" class="btn btn-primary">Go to Login Page</a>
                                </div>
                            </div>
                        <% } else { %>
                            <p class="text-muted">Enter your registration information to recover your password:</p>
                            
                            <form method="POST" action="/forgot-password">
                                <div class="mb-3">
                                    <label class="form-label">Mobile Number</label>
                                    <input type="tel" class="form-control" name="mobile" required>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Email Address</label>
                                    <input type="email" class="form-control" name="email" required>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Date of Birth</label>
                                    <input type="date" class="form-control" name="dob" required>
                                </div>

                                <div class="d-grid gap-2">
                                    <button type="submit" class="btn btn-warning">Recover Password</button>
                                </div>
                            </form>
                        <% } %>

                        <div class="text-center mt-3">
                            <p><a href="/login">Back to Login</a></p>
                            <p><a href="/">Back to Home</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
